# =============================================================================
# Write a function `safe_divide(a, b)` that:
#   - returns the result of a / b
#   - returns None if b is zero (catch the exception, don't crash)
#   - always prints "done" after the attempt (use finally)
# =============================================================================

def try_except_task():
    pass
