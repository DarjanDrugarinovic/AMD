# =============================================================================
# Create a type alias `Username` for str.
# Write a function `greet_user(username: Username) -> str` that returns
# "Welcome, {username}!". Call it and print the result.
# =============================================================================

type Username = str

def type_alias_task():
    pass
