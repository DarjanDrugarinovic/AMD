# =============================================================================
# Create a `Rectangle` class with `width` and `height` attributes.
# Add an `area()` method and a `perimeter()` method.
# Print both for a rectangle of your choice.
# =============================================================================

def basic_class_task():
    pass
