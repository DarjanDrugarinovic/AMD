# =============================================================================
# Write a `Point` class with `x` and `y` attributes, both typed as float.
# Add a method `distance_from_origin()` that returns the distance using
# the Pythagorean theorem. Add type hints to all methods.
# =============================================================================

def type_hints_task():
    pass
