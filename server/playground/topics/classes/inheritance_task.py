# =============================================================================
# Create a `Shape` base class with a `describe()` method.
# Subclass it as `Circle` and `Square`, each overriding `describe()`.
# Loop over a list of mixed shapes and call describe() on each.
# =============================================================================

def inheritance_task():
    pass
