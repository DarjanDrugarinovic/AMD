# =============================================================================
# Create a union type `StringOrInt = str | int`.
# Write a function `double(value: StringOrInt)` that:
#   - returns value * 2 if it's an int
#   - returns value + value if it's a str
# Test it with both types and print the results.
# =============================================================================

def union_type_task():
    pass
